Configuration settings kept on a higher abstraction level
Main, collections & factories have knowledge of the configuration in config.h.

Collections use config.h only to:
    - Start the concrete factory.

Other classes should be abstracted from it.

Collections can be responsible for populating their collection.

C:\Users\jarno.RATATOSK.000\.platformio\packages\toolchain-atmelavr\bin\avr-nm -Crtd --size-sort C:\arduino-devices\spacemouse\.pio\build\micro\firmware.elf > test_variables.txt